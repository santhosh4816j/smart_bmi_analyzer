<?xml version="1.0" encoding="utf-8"?>
<!-- Shown as the app boots, before Flutter draws its first frame.
     Matches the app's "sand" background color from the Beach Weather palette. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@android:color/white" />
</layer-list>
